# 지역사회의 이해 발표 일정 관리 프로젝트 정보 (PROJECT_INFO.md)

이 문서는 발표 일정 데이터의 파싱부터 **실시간 클라우드 연동 인터랙티브 대시보드** 구축까지 적용된 모든 기술적 기능과 운영 규칙을 정리한 문서입니다.

## 🌟 실시간 협업 시스템 (Real-time Collaborative App)

본 프로젝트는 정적 HTML의 한계를 넘어, 모든 사용자가 실시간으로 데이터를 공유하고 수정할 수 있는 환경을 제공합니다.
- **클라우드 연동**: Supabase DB를 통해 모든 수정 사항이 즉시 중앙 서버에 저장됩니다.
- **실시간 동기화**: 누군가 기업을 선택하거나 이름을 적으면, 다른 모든 접속자의 화면이 **새로고침 없이** 즉시 갱신됩니다.
- **동적 상태 관리**: 타인이 이미 선점한 기업은 드롭박스 상에서 취소선과 함께 비활성화(`disabled`) 처리되어 중복 선택을 방지합니다.

## 📋 핵심 기능 (Features)

### 1. 데이터 파싱 및 엑셀 생성
- **원본 데이터 정제**: 텍스트 형태의 발표 신청 데이터를 파이썬 스크립트를 통해 구조화된 데이터로 변환.
- **가로/세로형 엑셀 지원**: 주차별 발표자를 가로 및 세로(Transposed) 레이아웃으로 자동 생성.
- **슬롯 패딩**: 각 주차 및 분류(공공/상장)별로 최소 4개의 슬롯을 유지하여 레이아웃 일관성 확보.

### 2. 댓글 기반 검증 및 타임스탬프 반영
- **스크린샷 기반 검증**: 3주차~13주차 댓글 스크린샷 텍스트를 추출하여 데이터 누락 및 오기재(이승주, 유태수 등) 수동/자동 보정.
- **댓글 시간 표시**: 각 발표자 이름 옆에 실제 댓글 작성 시간(예: `3.8. 14:15`)을 표시하여 선점 순서 확인 가능.

### 3. 인터랙티브 HTML 대시보드 (index.html)
- **Glassmorphism 디자인**: 현대적이고 세련된 UI 스타일 적용.
- **반응형 테이블**: 긴 기업명이 잘리지 않도록 가변 너비 및 자동 조정 기능 적용.
- **정렬 표준화**: 데이터가 없는 "선택 안함" 라인도 실제 데이터 행과 동일한 너비와 정렬(이름 칸, 시간 태그 공간 등)을 유지.

### 4. 실시간 충돌 및 중복 관리 로직
- **댓글 시간 비교**: 동일 기업 중복 선택 시, 댓글 시간이 더 늦은 인원에게 자동으로 `(변경필요)` 태그 부여.
- **자동 상태 업데이트**: `(변경필요)` 태그가 붙은 인원은 드롭박스 초기 선택값이 자동으로 `"변경 필요"`로 설정됨.
- **실시간 UI 반영**: 드롭박스에서 특정 기업을 선택/해제할 경우, 다른 모든 슬롯의 드롭박스 목록에서도 해당 기업의 취소선 표시 및 비활성화(`disabled`) 여부가 실시간으로 갱신됨.

---

## ⚖️ 운영 지침 (Guidelines)

1. **중복 선점 방지**: 실시간으로 연동되는 드롭박스 옵션을 통해 이미 타인이 선택한 기업은 선택이 불가능함.
2. **변경 필요 대상 관리**: `(변경필요)` 태그가 유지된 슬롯은 타 수강생과 겹치지 않는 새로운 기업으로 수정을 유도함.
3. **레이아웃 유지**: 데이터 유무에 관계없이 테이블의 모든 행은 일관된 레이아웃(너비, 태그 공간 등)을 유지함.
4. **엄격한 지시 이행**: 모든 기능 변경은 요청자가 명시적으로 시킨 사항에 대해서만 수행함. (시킨 것만 하기)
5. **파일 관리**: 실시간 협업 버전인 **`index_realtime.html`**을 메인 대시보드로 사용함.
